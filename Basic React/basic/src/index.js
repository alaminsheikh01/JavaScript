import React from "react";
import ReactDOM from "react-dom/client";
import Work_api from "./Work_api";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Work_api />
  </React.StrictMode>
);
