/**
 * check permutaion
 * Given two strings, write a method to decide if one is a permutaion of the other.
 *
 * algorithm
 *
 * 1. check if they are the same exact strings - else
 * 2. check the lengths of both string - else
 * 3. create objects character count are equal - else
 * 4. return true
 */
